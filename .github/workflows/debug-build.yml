name: Android Debug Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          
      - name: Setup Gradle and Generate Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Generate Gradle wrapper 8.9.1
        run: |
          curl -L https://services.gradle.org/distributions/gradle-8.9.1-bin.zip -o gradle.zip
          unzip gradle.zip
         ./gradle-8.9.1/bin/gradle wrapper --gradle-version 8.9.1
          chmod +x gradlew

      - name: Build debug APK
        run:./gradlew assembleDebug

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/*.apk
